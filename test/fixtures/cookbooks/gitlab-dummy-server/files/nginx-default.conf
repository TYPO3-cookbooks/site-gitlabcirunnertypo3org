server {
	listen 80 default_server;
	listen [::]:80 default_server;

	server_name _;

	location /api/v4/runners {
		default_type application/json;
		return 201 '{"token": "autogeneratedtoken"}';
	}
}
